# Data Sources

AirTrace does not require external datasets by default.

If future features add optional datasets (maps, ephemerides, or sensor logs),
document the source, license, download steps, and local storage path here.
