# Debug-admin launch config for local testing.
# All unspecified values inherit the built-in defaults used by sim_default.cfg.

config.version=1.0
provenance.run_mode=simulation
provenance.allowed_inputs=simulation
provenance.allow_mixed=false
provenance.unknown_action=deny
policy.debug_admin.enabled=true
policy.debug_admin.start_active=true
